<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My List</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <main>
        <h1 class=""><%= listtitle %></h1>
        <section class="newItemEntry">
            <h2 class="offscreen">New Item Entry</h2>
    
            <form class="newItemEntry__form" id="itemEntryForm" action="/api/v1/todo" method="post">
                <label for="newItem" class="offscreen">Enter a new to do item</label>
                <input type="text" class="newItemEntry__input" id="newItem" maxlength="40" autocomplete="off"
                placeholder="Add item" name="newItem"/>
                <button id="addItem" class="button newItemEntry__button" 
                type="submit" name="list" value = <%= listtitle %>
                >
                +
                </button>
            </form>
    
    </section>
    
    <section class="listContainer">
        <header class="listTitle">
          <h2 id="listName">List</h2>
          <form class="newItemEntry__form" id="itemEntryForm" action="/api/v1/todo?_method=DELETE" method="post">
            <button id="clearItemsButton"  class="button listTitle__button" title="Clear the list"
            aria-label="Remove all items from the list">
            Clear
          </button>
        </form>
        </header>
        <hr />
        <ul id="listItems">
            <% for(let item of newListItems){%>
                <li class="item">
                    <p> <span class="em-dash">—</span> <%= item.descrip %></p>
                </li>
            <% } %>
        </ul>
      </section>
    </main>
</body>
</html>